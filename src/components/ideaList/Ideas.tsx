import styled from 'styled-components'

export const Ideas = () => {
  return <StyledSection>IdeaList</StyledSection>
}

const StyledSection = styled.main`
  position: relative;
  display: flex;
  flex-direction: column;
  align-items: center;
  width: 100%;
`
